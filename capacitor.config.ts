import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Angular Ionic Project',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
